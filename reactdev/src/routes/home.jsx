import { NavLink } from "react-router-dom"
import { Container } from "react-bootstrap"

export default function Home() {
    return (
        <div>
            <div className="">
                <Container>
                    Tattooing is hard enough. Scheduling, forms, and client notes shouldn't be.
                </Container>
            </div>
        </div>
    )
}
